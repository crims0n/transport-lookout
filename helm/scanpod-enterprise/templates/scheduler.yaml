apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "transport-lookout.fullname" . }}-scheduler
spec:
  replicas: 1
  strategy: { type: Recreate }
  selector: { matchLabels: { app: {{ include "transport-lookout.fullname" . }}-scheduler } }
  template:
    metadata: { labels: { app: {{ include "transport-lookout.fullname" . }}-scheduler } }
    spec:
      serviceAccountName: {{ include "transport-lookout.serviceAccountName" . }}
      securityContext: { runAsNonRoot: true, seccompProfile: { type: RuntimeDefault } }
      nodeSelector: {{ toYaml .Values.nodeSelector | nindent 8 }}
      containers:
        - name: scheduler
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["python", "-m", "scanpod_enterprise.scheduler"]
          securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] } }
          resources: {{ toYaml .Values.resources.scheduler | nindent 12 }}
          env:
{{ include "transport-lookout.env" . | nindent 12 }}
